Registre a aprovação da copy que está sendo discutida nesta conversa, seguindo o ramo "Se aprovada" da skill `skills/14-final-delivery-feedback/SKILL.md`.

Antes de registrar, confirme que a versão atual passou pela skill `13-copy-review-scorecard` com Score Geral ≥ 8, Score de Humanização/Anti-Vícios de IA ≥ 8, zero vício crítico e Knowledge Gate concluído. Se não houver scores válidos para esta versão, rode a skill `13` primeiro; `/aprovar-copy` nunca pula a revisão.

Atualize `clients/{cliente}.md`, salve a copy em `outputs/approved/` e um exemplo em `examples/approved/`, usando o template de `quality/feedback-template.md`.
